.DS_Store
graphify-out/
.env*
!.env.example
# Cursor IDE local state (hooks, workspace cache — not shared across machines)
.cursor/

# Claude Code local settings (may contain API keys and machine-specific paths)
.claude/

node_modules/
build/
dist/